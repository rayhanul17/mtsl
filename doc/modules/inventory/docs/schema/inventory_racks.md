# Table: inventory_racks

```yaml
---
id: tbl:inventory_racks
module: inventory
status: inferred
model: InventoryRack
columns: [id]
---
```

PostgreSQL schema: `inventory_module`. PK `id` for FK targets.
